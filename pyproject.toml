[project]
name = "customer-support-openenv"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "requests",
    "openai",
    "openenv-core>=0.2.0"
]

[tool.uv]

[project.scripts]
server = "server.app:main"